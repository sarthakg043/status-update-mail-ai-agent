# GitHub Configuration
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_USERNAME=your_github_username

# GitHub Repositories (optional, comma-separated, format: owner/repo)
# When set, fetches PRs directly from these repos using the pulls API.
# This is required for private repos you have access to but don't own.
# When not set, uses the GitHub search API (may not find PRs in private repos).
GITHUB_REPOS=

# Google Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: Specify the Gemini model to use (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Email Configuration (Choose one: Gmail or Zoho)
# For Gmail with App Password
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_APP_PASSWORD=your_gmail_app_password

# For Zoho Mail
# EMAIL_SERVICE=zoho
# EMAIL_USER=your_email@zohomail.com
# EMAIL_APP_PASSWORD=your_zoho_app_password

# Email Recipients (comma-separated)
EMAIL_TO=recipient1@example.com,recipient2@example.com

# Email Subject
EMAIL_SUBJECT=Daily Status Update

# Date Range for PR Fetch (leave empty for today)
# Format: YYYY-MM-DD
START_DATE=
END_DATE=

# User Instructions for Email Content
USER_INSTRUCTIONS=Please draft a professional status update email summarizing my pull requests. Include PR titles, descriptions, and current status.

# Include Code Changes in PRs (default: true)
# Set to 'false' to exclude code snippets from the email generation
# INCLUDE_CODE=true

# Maximum characters per file in code snippets (default: 500)
# Adjust this value to control how much code is included for each file
# MAX_CHARS_PER_FILE=500

# Runtime Configuration
NODE_ENV=development
PORT=3000

# Database MongoDB
# Enable DB-backed features (set to 'true' to enable)
DATABASE_ENABLED=false
MONGODB_USERNAME=
MONGODB_PASSWORD=
# Optional: MongoDB cluster host and database name
MONGODB_HOST=status-update-ai-main.0mr0frx.mongodb.net
MONGODB_DATABASE=status-updates

# Internal Worker / Scheduler Authentication
INTERNAL_WORKER_SECRET=

# Clerk Authentication
# Get these from: https://dashboard.clerk.com → API Keys
CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Webhook Security
# Get this from: Clerk Dashboard → Webhooks → <your endpoint> → Signing Secret
CLERK_WEBHOOK_SECRET=whsec_clerk_webhook_secret

# Stripe Billing
STRIPE_SECRET_KEY=sk_test_or_live_key
STRIPE_WEBHOOK_SECRET=whsec_stripe_webhook_secret
# Local development / testing only (DO NOT commit real values)
# Used by scripts/get-test-token.js to obtain a Clerk JWT for manual API testing
# TEST_USER_EMAIL=you@example.com
# TEST_USER_PASSWORD=your_test_password